import React from 'react'

const ContactList = (props) => {
    const {contactsArray} = props
    const contactJSX = contactsArray.map (contact >= 
        (
      <div>
        <div>bankIcon
        <img
                className="contact-bankIcon__img"
                src={contact.bankIcon}
                alt={contact.name}
                            />
        </div>
        <div>{contact.name}        </div>
        <div>{contact.sortCode}</div>
        <div>{contact.Account</div>
        <div>{contact.bankName}</div>
        <div>{contact.iban}</div>
        <div>DeleteImage</div>
      </div>
  

    return (
    <div>ContactList</div>
  )
}

export default ContactList